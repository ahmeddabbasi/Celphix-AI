import { Hash } from "lucide-react";
import { PaygPlaceholder } from "./_PaygPlaceholder";
export default function PaygNumbers() {
  return <PaygPlaceholder title="Numbers" subtitle="Manage your PAYG phone numbers." Icon={Hash} />;
}
