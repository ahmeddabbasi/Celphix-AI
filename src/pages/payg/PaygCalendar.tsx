import { Calendar } from "lucide-react";
import { PaygPlaceholder } from "./_PaygPlaceholder";
export default function PaygCalendar() {
  return <PaygPlaceholder title="Calendar" subtitle="Schedule and manage PAYG call events." Icon={Calendar} />;
}
