import { Hash } from "lucide-react";
import { PaygPlaceholder } from "../_PaygPlaceholder";
export default function PaygNumbersCustomSip() {
  return <PaygPlaceholder title="Custom SIP" subtitle="Configure custom SIP trunks." Icon={Hash} />;
}
