import { Hash } from "lucide-react";
import { PaygPlaceholder } from "../_PaygPlaceholder";
export default function PaygNumbersTwilio() {
  return <PaygPlaceholder title="Twilio" subtitle="Connect your Twilio numbers." Icon={Hash} />;
}
